// configUI.js
const UI_CONFIG = {
    panel: {
        // Géométrie
        xp: 25,
        yp: 25,
        sp: 50,
        aspectRatio: 1.5,

        // État
        visible: true,
        debug: true,

        // Interactions
        isDraggable: true,
        isZoomable: true,

        // Comportements
        toggleOnClick: true,
        toggleShortcut: "v"
    }
};
